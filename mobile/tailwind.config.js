/**
 * HeroUI Native v1 uses Tailwind CSS v4's CSS-first integration. Its styles
 * are loaded from src/global.css, so no legacy Tailwind plugin is required.
 */
module.exports = {
  darkMode: 'class',
};
